{
  "name": "phaser-adventure-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:sanity": "echo \"Manual sanity checks (boot/dialog/collect)\"",
    "test:smoke": "playwright test tests/smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  }
}
