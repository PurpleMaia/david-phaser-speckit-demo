// Manual sanity script: run `npm run dev`, open the game, and verify:
// - Game boots to single room
// - Player sprite is visible at spawn and responds to WASD/arrow keys
// - Player cannot leave the room or clip through walls
console.info('Manual sanity: boot and movement — see README for steps.');
